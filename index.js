const gameController = function () {
    const playGame = () => ()
  return;
};

const gameboard = function () {
  const board = [
    ["", "", ""],
    ["", "", ""],
    ["", "", ""],
  ];

  const updateBoard = (marker, index) => {board[index] = marker};
  return board;
};

// const Player =

// }

gameController.startGame();
